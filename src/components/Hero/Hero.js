import React from "react";
import "./Hero.css";

const Hero = () => (
  <div className="hero">
    <h1>Presidents of the United States Memory Game!</h1>
    <h3>Click a Prez to earn points, but don't click more than once!</h3>
  </div>
);

export default Hero;